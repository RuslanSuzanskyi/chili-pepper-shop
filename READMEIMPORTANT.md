create an .env in backend folder and add:
MONGODB_URI=mongobd_key
PORT=5000

create an .env in frontend folder and add:
VITE_API_URL=http://localhost:5000

start server by typing node server.js in backend folder 
